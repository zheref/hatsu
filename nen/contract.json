{
  "$schema": "nen.contract/v0.1",
  "$comment": "Authoritative Hatsu/Nen dependency and execution declaration. Current feature minimum is 0.14 and published bootstrap ref v0.14.0; review-round-only approval policy is required by Hatsu's reviewer gate. Nen 0.10.0 preserves compatibility floor 0.7 for older consumers. Read the installed binary verdict through nen shu tools; do not recompute compatibility in skill prose.",
  "contract": "hatsu-nen-dependency",
  "contract_version": 2,
  "decision": "D10",
  "authority": "THIS FILE IS THE SINGLE SOURCE OF TRUTH for the Nen dependency. Any version, ref, URL or command echoed in README.md, docs/, an agent definition or a SKILL.md is a convenience copy of a value that lives here. Where a copy disagrees with this file, this file wins and the copy is the bug. Read the values from here at run time; never act on a remembered version. There is exactly one copy of this contract, at nen/contract.json. A second file carrying the same pin would be a second source of truth, which is the thing this clause forbids.",
  "summary": "Hatsu depends hard on the Nen CLI. Every deterministic step is a Nen verb. The dependency fails closed with auto-install: out of the declared range or absent, the checksum-verified bootstrap runs itself; only if the bootstrap fails does the session halt with the exact command. A Nen-owned operation is NEVER improvised in prose.",
  "dependency": {
    "name": "nen",
    "source": "zheref/nen",
    "repository": "https://github.com/zheref/nen",
    "minimum": "0.14",
    "minimum_semantics": "MAJOR.MINOR capability minimum, checked by nen shu tools against the installed binary and its compatibility floor. Raise this for either a breaking compatibility floor or a newly adopted feature unavailable in earlier releases. Hatsu adopts, FROM v0.14.0 (zheref/nen#242): nen wc swap and nen wc worktrees, the hatsu:amenotejikara engine. Hatsu adopted, FROM v0.13.0 (zheref/nen#227): the antigravity and claude-code surface rows with --models, --permissions, --hooks, --rules and --stamp, nen surface mirror check --installed, nen usage record, shu step durations in the phase ledger, phases[] and usage[] in report data, nen wc catch-up, nen wc publish, nen commit write, nen pr open and the profile key -- so its minimum was 0.13 then, and is 0.14 now, even though Nen keeps floor 0.7. PRIOR ADOPTION, from v0.12.0 (zheref/nen#220): objects[] in nen report data, nen review scopes, nen pr threads, nen report render --variant and --graph, nen report mermaid; from v0.11.0 (zheref/nen#216) and still required: the decision matrix (nen/decisions.json), the rich stop (nen stop --options/--propose-issue, nen.stop.mark/v0.2), nen shu warmup --carry, nen repo classify, nen surface capabilities and nen phase. A minimum names the OLDEST release carrying every verb Hatsu executes, so it moves to the newest adoption and never back. Never treat an older binary that accepts unknown JSON keys as proof it executes the new capability.",
    "zero_major_caveat": {
      "applies_while": "the installed and pinned majors are both 0",
      "rule": "For a pin 0.A and installed build 0.B.z with compatibility floor 0.F, Nen accepts A <= B and A >= F, bounded by that build's own minor. Current Hatsu minimum 0.14 with Nen 0.14.0/floor 0.7 is accepted; Nen 0.13.x is older than the required feature minimum. Legacy minimum 0.7 remains compatible with 0.14.0, but that does not prove an older binary supports nen wc swap. Read the exact range from nen shu tools. An unparseable version is unknown and unsatisfied, not a pass.",
      "why": "SemVer 2.0.0 clause 4: at major version zero the public API is unstable and anything MAY change at any time, so at 0.x the MINOR is the breaking-change vehicle. Read at its strictest that made '0.7' mean '>=0.7.0 <0.8.0' EXACTLY, and every minor — breaking or not — owed a repin PR in every consuming repository; a repin owed unconditionally is a repin nobody reads. The maintainer's ruling of 2026-09-10 narrows it to 'exact minor is fine, unless there is a breaking change', and the fact that makes the distinction decidable is one the binary SHIPS rather than one a consumer infers: nen's release step sets COMPATIBLE_MINOR_FLOOR to a release's own minor when that release's CHANGELOG '### Breaking / consumer notes' section carries a real bullet, and leaves it where it stands when it carries none (zheref/nen#200; docs/USAGE.md 'the compatibility floor' and 'the release-time floor step', guarded by src/version.test.ts, which reads the topmost released section and fails the build when the two disagree). So the floor is what says which releases broke something, carried as data into a checkout that no longer exists rather than derived from prose at run time. It is a floor and never a ceiling, and it only ever WIDENS what is accepted — which is why nen's own notes for it record that it is not itself a breaking change. The floor is seeded at 0.7 because v0.7.0's notes ARE breaking, and those four changes are the reason this contract originally adopted minimum 0.7 (since raised to 0.14 for adopted features) at all: `nen stage triage` gained the `local-config` and `large` detectors, so a working copy that answered exit 0 answers exit 1 on the same bytes; every relative own-path flag resolves against `--repo`'s root instead of the PROCESS's directory, so one unchanged command line run from a worktree reads a different file; a missing or malformed `--target` exits 2 rather than 1 across sixteen verbs, and so does an unreadable caller-named input on `split verify`, `changelog` and `canon mirror check`; and `nen pr ready` READS `nen/gates.json`'s `dependabot_carve_out`, so a pull request that answered `not-ready` can answer `ready` against an unchanged file. Each is a silent change of meaning rather than a new flag, which is exactly what a floor exists to record.",
      "on_a_higher_minor": "A newer release that keeps a compatibility floor admitting this feature minimum may satisfy the pin without a change. A raised floor above minimum requires an explicit compatibility repin. An installed binary older than minimum needs the published pinned_ref. Read the Nen verdict rather than downgrade a matching newer binary.",
      "from_1_0_onward": "Once nen reaches 1.0, minimum 'X.Y' means >=X.Y.0 <(X+1).0.0 — backward-compatible WITHIN the major, so a higher minor or patch satisfies it and the compatibility floor is not consulted at all. nen already answers that way for a 1.x pin today; this contract is bumped to say so when nen's own line gets there."
    },
    "pinned_ref": "v0.14.0",
    "pinned_ref_semantics": "Published bootstrap release adopted after verifying its binaries and SHA256SUMS. v0.14.0 adds nen wc swap (--take, --return, --status) and nen wc worktrees (zheref/nen#242, released by #246), the engine hatsu:amenotejikara runs, and keeps compatibility floor 0.7; Hatsu raises minimum to 0.14 for those verbs. Before it, v0.13.0 implemented the unified surface generator rows, the usage ledger, shu step durations, the stall rule and the four git verbs (Nen #227) on top of v0.12.0 (Nen #220) and keeps compatibility floor 0.7. Hatsu raised minimum to 0.13 for those verbs (zheref/nen#227). minimum and pinned_ref still move independently when no new feature or breaking floor requires a minimum change; never use latest or an unpublished ref.",
    "pinned_ref_bootstrap_caveat": "A TAG IS NOT A RELEASE, and this is the durable rule for EVERY value `pinned_ref` ever takes, not a note about one of them. `bootstrap/nen.sh` resolves the binary and its SHA256SUMS from the GitHub RELEASE at --ref; the tag's own tree carries the script but no assets. TWO CASES, AND THEY FAIL AT DIFFERENT STEPS. A `pinned_ref` whose TAG EXISTS but whose RELEASE is not published exits 6 (EXIT_MANIFEST — SHA256SUMS unfetchable) on both install_paths. A `pinned_ref` whose TAG DOES NOT EXIST YET NEVER REACHES EXIT 6 AT ALL: `raw.githubusercontent.com` 404s on the script itself, so `curl -f` exits 22 (or 56 mid-transfer), no file is created, and the second step never runs. Saying `exit 6` for both was wrong in a way that costs debugging time — an operator looking for a manifest failure when what is missing is the tag. Either way the warm-up halts per `halt`. That is the fail-closed guard working rather than a defect in this file, and exit 6 is one of the two codes that is NEVER retried. THE RULE THIS IMPLIES: never move `pinned_ref` to a ref whose release is unpublished without knowing that the halt is the cost. CI DOES SEE IT, and that sentence used to say the opposite: `.github/workflows/surface-mirror-check.yml` reads `dependency.pinned_ref` out of this file and bootstraps it, so a pull request that moves the pin ahead of the release goes RED at that step — `curl` 404s on the raw script, or the script exits 6 on an unfetchable SHA256SUMS — on every pull request, not only the one that moved it. That is the guard working where it can be seen rather than only on a developer's machine, and it is a reason to sequence the repin behind the release rather than a reason to weaken either. Publishing the release is the nen maintainer's act and the only thing that clears it.",
    "version_probe": [
      "nen",
      "--version"
    ],
    "version_probe_semantics": "An argv ARRAY, never a string: nen's loader refuses the string form (verified: `at dependency.version_probe, expected an argv ARRAY, got the string \"nen --version\"`), because a string is one 'sh -c' away from a shell. The warm-up runs exactly these elements, with no shell in between.",
    "version_probe_output": "A bare semver on stdout, e.g. '0.10.0' — no 'v' prefix, no other text. The probe answers WHAT IS INSTALLED; whether that satisfies `minimum` is `nen shu tools`'s verdict and not this string's.",
    "bootstrap": {
      "vendored": false,
      "vendoring_forbidden": "Hatsu never carries a copy of this script. A vendored copy is a second, unreviewed supply chain that drifts from the manifest it verifies against. Hatsu fetches nen's own published script at the pinned ref, every time.",
      "script_path_in_source": "bootstrap/nen.sh",
      "url": "https://raw.githubusercontent.com/zheref/nen/v0.14.0/bootstrap/nen.sh",
      "fetch": "curl -fsSL https://raw.githubusercontent.com/zheref/nen/v0.14.0/bootstrap/nen.sh -o <local-file>",
      "fetch_must_be_two_step": {
        "rule": "ALWAYS fetch to a file, then run that file. NEVER pipe the script into bash.",
        "why": "The script runs under `set -u` and reads ${BASH_SOURCE[0]}. Piped into `bash -s --` there is no BASH_SOURCE, so it dies with `BASH_SOURCE[0]: unbound variable` and exits 1 — a code that appears in NO table here, from a script that never reached its own argument parsing. It looks like an ordinary failure and is not one; it means the invocation form was wrong. Reproduced against this contract's pinned ref.",
        "wrong": "curl -fsSL <url> | bash -s -- --ref v0.14.0",
        "right": "curl -fsSL <url> -o /tmp/nen-bootstrap.sh && bash /tmp/nen-bootstrap.sh --ref v0.14.0"
      },
      "flags": {
        "--ref": "Required, no default. The tag to pin to.",
        "--source": "owner/name to fetch release assets from. Defaults to zheref/nen.",
        "--cache-dir": "Cache root. Defaults to ${XDG_CACHE_HOME:-$HOME/.cache}/nen."
      },
      "stdout": "On success, the resolved and checksum-verified binary path — and nothing else. Execute exactly that path.",
      "exit_codes": {
        "0": "verified — the path on stdout is safe to execute",
        "2": "EXIT_USAGE — the invocation is wrong; nothing was attempted",
        "3": "EXIT_UNSUPPORTED_HOST — this platform/arch ships no nen binary",
        "4": "EXIT_DOWNLOAD — the binary asset could not be retrieved (the retryable one)",
        "5": "EXIT_CHECKSUM — SECURITY: bytes did not verify, or could not be verified",
        "6": "EXIT_MANIFEST — SHA256SUMS unfetchable, missing, malformed, or silent about the artifact",
        "7": "raised by `nen bootstrap` only, never by the script: no bootstrap/nen.sh found on disk. Not a supply-chain failure — a wiring failure. Supply --script.",
        "curl 22 / 56 (before the script runs)": "THE SCRIPT ITSELF could not be fetched at the pinned ref and NO FILE WAS CREATED, so no exit code above was ever raised: `curl -f` exits 22 on the 404 and 56 on a mid-transfer failure. It means the tag or the release does not exist yet -- most often a pin moved ahead of the nen release. NEVER RETRIED: a tag that is absent now is absent on the next attempt. The fix is the nen release being published, which is the nen maintainer's act; nothing on this side clears it."
      },
      "retry_policy": "A curl 22/56 before the script runs is NEVER retried -- the tag or release is absent and will be until it is published. Exit 4 may be retried once. Exits 5 and 6 are NEVER retried — retrying a checksum failure is how a fail-closed guard becomes a fail-open one by attrition. Exit 3 will not change on this host. Exits 2 and 7 are invocation errors: never retry the same command, but the FIXED invocation (correct usage; for 7, supply --script) is expected to succeed — fix and re-run rather than halting on them.",
      "published_binaries": [
        "nen-linux-x64",
        "nen-darwin-arm64",
        "nen-windows-x64.exe"
      ],
      "manifest_asset": "SHA256SUMS"
    }
  },
  "project": {
    "$comment": "What nen EXECUTES for this repository — the other half of the two-file model documented in docs/WORKFLOW.md, whose policy half is nen/workflow.json. `nen shu detect --repo .` answers exit 1, 'no lane detected': Hatsu is bash-and-markdown and matches none of nen's seven reference stacks, so this block is written BY HAND, as kurapika.md § 'The shu verbs' requires. The repository lane declares its real lint argv and seats its absent verbs; the separate guard lane declares the one focused fixture. A seat is exit 4 — a fact, not a failure — and an omitted verb would be exit 2, which reads as a broken declaration rather than as a deliberate absence.",
    "lanes": {
      "plugin": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The repository lane: a Claude Code plugin whose shipped surface is .claude-plugin/, claude/, nen/, contracts/, docs/ROSTER.md, docs/delegation-grammar-DRAFT.md, docs/WORKFLOW.md, docs/DISCOVERY.md, docs/LAUNCH-MIGRATION.md, docs/STANDALONE-ENTRY.md, docs/PROCESS.md, docs/SURFACES.md and hooks/. The stack id is Hatsu's own name for it, not one of nen's seven — nen carries no build system and validates the id against nothing."
      },
      "plugin-bump-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The one static focused lane for the executable plugin-version guard. Its declared fixture exercises only scripts/plugin_bump_check.sh's runtime-document classification and version result; it is not Hatsu's general regression suite or a substitute for an unavailable dynamic test selector."
      },
      "plugin-update": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/hatsu_plugin_update.sh. Its declared fixture builds throwaway origin/consumer clones and asserts trunk ff, release catch-up, dirty/authoring/cache/no-origin skips, GIT_DIR isolation, and the no-stable-tag refuse. It is not a general suite."
      },
      "tenkai-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/tenkai_adopt.sh, the hatsu:tenkai engine. The repository's pattern is ONE focused lane per executable guard -- the `test` seat on the `plugin` lane says so in its own words: 'Executable guards have their own focused lanes'. The engine shipped with a --self-test and no lane, so its assertions ran only when somebody typed the command: tsukuyomi had nothing to route to and no CI job covered it. An unrun check is exactly what that seat refuses to let read as a clean one."
      },
      "workflow-policy-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/workflow_runner_policy_check.rb, the structural, offline regression guard three PR-facing workflows (plugin-bump-check.yml, surface-mirror-check.yml, pr-readiness.yml) and the surface-mirror-regenerate.yml builder all invoke as a trusted-revision step. Same one-lane-per-executable-guard pattern as tenkai-guard and release-guard: the guard shipped with a --self-test and no lane, so its own negative fixtures ran only when a human typed the command by hand."
      },
      "release-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/release-publish.sh, following this repository's one-lane-per-executable-guard pattern. The publisher's fixtures are hermetic and offline: they build a throwaway git repository with two tags and a changelog, and prove notes composition, the multi-version gap case, the DERIVED tag, and every refusal -- publishing nothing, ever."
      },
      "dist-tag-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/dist_tag.sh, kagutsuchi's distribution-tag block, following this repository's one-lane-per-executable-guard pattern. Its fixtures are hermetic and offline: they build a throwaway git repository with its own nen/workflow.json and identity file, and prove every refusal the guard exists to make -- no tags.deploy entry (exit 3), push:false dropping --push, a symlinked and an outside-repo nameFrom, an unreadable or missing target file, a target file that is not one printable line, a non-hex and a short build SHA, a dirty tree, an illegal composed ref, and a two-arg flag given no argument. It cuts nothing: every assertion runs through --dry-run or a refusal that fires before `nen tag cut` is reached."
      },
      "report-time-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/report_time.sh, which says a report's UTC generatedAt in the zone the report is for (docs/WORKFLOW.md, Report time), following this repository's one-lane-per-executable-guard pattern. Its fixtures are hermetic and offline: fixed instants in fixed zones prove the local clock, the offset, the date flip on either side of UTC and a half-hour zone, and every refusal -- an unknown zone, a path posing as one, a non-UTC or unparseable instant, a missing or unknown argument. It writes nothing."
      },
      "pr-body-guard": {
        "stack": "claude-code-plugin",
        "cwd": ".",
        "$comment": "The focused lane for scripts/pr_body_evidence_check.sh, which refuses a pull request body whose screenshots are not cells of the UZF-26 evidence table or carry no alt text (docs/WORKFLOW.md, The UZF-26 evidence shape, The table exactly). One lane per executable guard. Its fixtures are hermetic and offline: bodies with no images, a markdown table and an HTML table of labelled images pass; a loose markdown image, a loose <img>, images stacked one per line, an unlabeled markdown image, an <img> with an empty or no alt, and a second unlabeled <img> on a labelled line are refused; an image inside a code fence is ignored; an unreadable body and an unknown argument are usage errors."
      }
    },
    "defaultLane": "plugin",
    "verbs": {
      "plugin": {
        "lint": {
          "exe": "claude",
          "argv": [
            "plugin",
            "validate",
            ".",
            "--strict"
          ],
          "$comment": "The host's own validator, run strictly, is the whole of Hatsu's mechanical verification: it parses both manifests, resolves every path plugin.json points at (agents, commands, skills, hooks), and fails on a surface the manifest claims but the tree does not carry. It is what workflow.json declares as the single `iteration.checks` entry, so this is exactly what breath proves on the fresh tip, what rasengan re-runs as its inner loop while authoring, and what kokusen runs over the finished tree before every commit."
        },
        "build": {
          "unsupported": "Hatsu compiles nothing. The plugin IS its source: markdown agent and skill definitions, two JSON manifests, one POSIX hook pair and one bash guard script, all read as-is by Claude Code. `lint` (claude plugin validate --strict) is the closest thing to a build and is declared as lint, where it belongs."
        },
        "test": {
          "unsupported": "No general automated test suite exists on the repository lane. Executable guards have their own focused lanes (plugin-bump-guard, plugin-update, tenkai-guard, release-guard, dist-tag-guard, report-time-guard, pr-body-guard); every other surface is prose read by an agent. Declaring a general runner that does not exist would make an unperformed check read as a clean one."
        },
        "ui-test": {
          "unsupported": "There is no user interface to drive. Hatsu's surface is a set of agent and skill definitions loaded by Claude Code; the only rendered artifact is templates/spiritual-message.html, which is a report template, not an application."
        },
        "coverage": {
          "unsupported": "Nothing here executes, so nothing here is covered. byakugan reports Hatsu's touched files as `not measurable here` against the ladder in nen/workflow.json rather than against a number, and the ladder still governs every repository Hatsu drives that declares none of its own. Gyo is linting."
        },
        "archive": {
          "unsupported": "A Claude Code plugin is distributed by git ref through a marketplace, never as a built package: `claude plugin marketplace add zheref/hatsu` reads the tree at a ref. There is no artifact to produce, so there is nothing for `archive` to name."
        },
        "release": {
          "exe": "bash",
          "argv": [
            "scripts/release-publish.sh",
            "--repo",
            "."
          ],
          "$comment": "RETIRED SEAT, maintainer's ruling 2026-09-19. This row previously read: \"Publication is a git tag plus the marketplace entry that already points at this repository — there is no publish step to run, and G3 (CON-6) holds the tag decision anyway. Emitter prepares a release here; nothing in this repository publishes one.\" -- and that became false the moment a GitHub Release was published by hand at v0.39.0. A declaration that disagrees with the practice is a bug in the declaration, and the cost was concrete: `hatsu:mugetsu` exists to execute this row at G3, so a seat here meant publication happened outside the machinery entirely. The row now runs scripts/release-publish.sh, which publishes ONE GitHub Release for a tag that already resolves on the remote. It cuts no tag (that is `nen tag cut`, hatsu:getsuga section 4), pushes nothing, builds nothing, and attaches only artifacts it is handed -- for this stack there are none, because a Claude Code plugin is distributed by git ref through a marketplace and the tag IS the distribution. --tag is omitted here deliberately: `nen shu release` runs this argv with no arguments, so the script derives the newest v* tag and names it as DERIVED in its output. Every refusal fires before anything is sent -- an absent tag, a tag missing from the remote, and a tag that already has a release. G3 (CON-6) still holds the go: this row is what mugetsu RUNS, never permission to run it."
        },
        "dev": {
          "unsupported": "There is no debug build to start. The development loop is `claude plugin marketplace add ./hatsu` against a local checkout followed by `claude plugin install hatsu@hatsu`, which is a host operation on the developer's own session, not a process this repository spawns."
        },
        "run": {
          "unsupported": "Same as `dev`: nothing here runs as a process. An installed Hatsu is loaded by Claude Code, and the way to exercise it is to invoke /kurapika in a session, which is a human act at a terminal nen does not own."
        },
        "deploy": {
          "unsupported": "Hatsu sends nothing anywhere. Distribution is the marketplace reading this repository at a ref, so there is no destination for --target to name and no credential for a deploy to carry. `project.targets` is empty for the same reason, and empty deliberately rather than by omission."
        }
      },
      "plugin-bump-guard": {
        "test": {
          "exe": "bash",
          "argv": [
            "scripts/plugin_bump_check_fixture.sh"
          ],
          "$comment": "A permanent, deterministic fixture for the guard's executable behavior. It creates synthetic changed-file lists and manifests in a temporary directory, invokes the checked-in guard, and removes that directory. Run it only as `nen shu test --lane plugin-bump-guard --repo <hatsu>` when the guard changes."
        }
      },
      "plugin-update": {
        "test": {
          "exe": "bash",
          "argv": [
            "scripts/hatsu_plugin_update_fixture_check.sh"
          ],
          "$comment": "A permanent, deterministic fixture for the plugin-source updater. Run it only as `nen shu test --lane plugin-update --repo <hatsu>` when scripts/hatsu_plugin_update.sh or its fixture changes."
        }
      },
      "tenkai-guard": {
        "test": {
          "exe": "bash",
          "argv": [
            "scripts/tenkai_adopt.sh",
            "--self-test"
          ],
          "$comment": "The hatsu:tenkai engine's own fixtures -- hermetic and offline. It builds throwaway git repositories under a temp dir, proves the runner derivation at every input, idempotence across three applies, every drift direction detected AND repaired, the read-only guarantee by content hash over the whole tree including .git/hooks, the two-PR ordering staged rather than hit, and that a hostile slug, a missing slug, a non-git directory and somebody else's workflow are each refused. Every fixture is removed on every path. Run it as `nen shu test --lane tenkai-guard --repo <hatsu>` when scripts/tenkai_adopt.sh or templates/pr-readiness.yml changes."
        }
      },
      "workflow-policy-guard": {
        "test": {
          "exe": "ruby",
          "argv": [
            "scripts/workflow_runner_policy_check.rb",
            "--self-test",
            "."
          ],
          "$comment": "The workflow runner policy guard's own negative fixtures -- hermetic and offline, against a scratch copy of .github/workflows/ under a temp dir. Run it as `nen shu test --lane workflow-policy-guard --repo <hatsu>` when scripts/workflow_runner_policy_check.rb or any .github/workflows/*.yml it governs changes."
        }
      },
      "release-guard": {
        "test": {
          "exe": "bash",
          "argv": [
            "scripts/release-publish.sh",
            "--self-test"
          ],
          "$comment": "Run it as `nen shu test --lane release-guard --repo <hatsu>` when scripts/release-publish.sh or templates/release-publish.sh changes."
        }
      },
      "dist-tag-guard": {
        "test": {
          "exe": "sh",
          "argv": [
            "scripts/dist_tag.sh",
            "--self-test"
          ],
          "$comment": "The distribution-tag guard's own fixtures -- hermetic and offline, run under /bin/sh because the script is POSIX sh. Every fixture is removed on every path. Run it as `nen shu test --lane dist-tag-guard --repo <hatsu>` when scripts/dist_tag.sh changes."
        }
      },
      "report-time-guard": {
        "test": {
          "exe": "sh",
          "argv": [
            "scripts/report_time.sh",
            "--self-test"
          ],
          "$comment": "The report-time helper's own fixtures -- hermetic and offline, run under /bin/sh because the script is POSIX sh. Run it as `nen shu test --lane report-time-guard --repo <hatsu>` when scripts/report_time.sh changes."
        }
      },
      "pr-body-guard": {
        "test": {
          "exe": "sh",
          "argv": [
            "scripts/pr_body_evidence_check.sh",
            "--self-test"
          ],
          "$comment": "The PR-body evidence check's own fixtures -- hermetic and offline, run under /bin/sh because the script is POSIX sh. Run it as `nen shu test --lane pr-body-guard --repo <hatsu>` when scripts/pr_body_evidence_check.sh changes."
        }
      }
    },
    "preconditions": {
      "plugin": [
        {
          "kind": "path",
          "value": ".claude-plugin/plugin.json",
          "why": "The plugin manifest is what `claude plugin validate --strict` validates; without it the verb has nothing to read and would fail on a missing file rather than on a real defect."
        },
        {
          "kind": "path",
          "value": ".claude-plugin/marketplace.json",
          "why": "The marketplace manifest is the second half of the shipped surface and carries its own description; --strict reads both, and a check that silently skipped one would be an unperformed check reported as a clean one."
        }
      ]
    },
    "hosts": {
      "*": [
        "darwin",
        "linux",
        "win32"
      ],
      "test": [
        "darwin",
        "linux"
      ],
      "$comment": "The plugin lane's lint runs through Claude Code on all three hosts. The focused test verb is narrower: the guard fixtures (scripts/plugin_bump_check_fixture.sh, scripts/hatsu_plugin_update_fixture_check.sh, scripts/tenkai_adopt.sh --self-test, scripts/release-publish.sh --self-test, scripts/dist_tag.sh --self-test, scripts/report_time.sh --self-test, scripts/pr_body_evidence_check.sh --self-test) use a POSIX shell, git and mktemp and are verified only on Darwin/Linux, not native Windows. The default plugin lane's test is unsupported, so this test allowlist applies only to those focused fixtures. The Stop bell in hooks/ is macOS-only, but a harness hook is not a nen verb and constrains no lane — it no-ops off Darwin rather than refusing."
    },
    "targets": {},
    "toolchain": {
      "claude": {
        "version": ">=2.0.0",
        "probe": [
          "claude",
          "--version"
        ],
        "versionFrom": "first-semver-on-stdout",
        "installer": "verify-only",
        "why": "Claude Code is both the host that loads this plugin and the program the `lint` verb spawns. It is verify-only on purpose: nen never installs the harness it is running inside, and the install path is the user's own (claude.com/claude-code)."
      }
    }
  },
  "install_paths": {
    "note": "TWO cases, TWO paths. Which one applies is decided by the probe, never by preference.",
    "nen_absent": {
      "when": "`nen --version` is not found on PATH, exits non-zero, or prints something unparseable — and equally when the binary on PATH is so old it has no `nen bootstrap` at all (exit 2, `unknown command`), since the re-pin path below presupposes that verb.",
      "path": "The shell bootstrap, directly.",
      "commands": [
        "curl -fsSL https://raw.githubusercontent.com/zheref/nen/v0.14.0/bootstrap/nen.sh -o <local-file>",
        "bash <local-file> --ref v0.14.0"
      ],
      "why_shell_here": "Chicken-and-egg, and it is the ONLY reason shell is permitted anywhere on this path. `nen bootstrap` is a nen subcommand, so invoking it presupposes the binary that is precisely what is missing. The script exists because a bootstrap written in the language its own output provides cannot run before that output exists. This is the sole carve-out; it does not generalize to any other operation."
    },
    "nen_present_out_of_range": {
      "when": "`nen shu tools --repo <this checkout>` answers the `nen` row `present-but-wrong-version` — the binary parses and does not satisfy `minimum` under dependency.zero_major_caveat's floor rule. The row is the verdict; the range is never recomputed here. TWO SHAPES, ONE PATH: a pin BELOW the build's compatibility floor (the row names the repin, and no build of that line will ever satisfy it — the fix is a pull request against this file, not an install), and a binary OLDER than the pin (the install below is the whole fix).",
      "path": "Re-pin THROUGH NEN'S OWN VERB. A working nen is on PATH, so the chicken-and-egg rationale does not apply and the shell path is not the one to reach for.",
      "commands": [
        "curl -fsSL https://raw.githubusercontent.com/zheref/nen/v0.14.0/bootstrap/nen.sh -o <local-file>",
        "nen bootstrap --ref v0.14.0 --source zheref/nen --script <local-file>"
      ],
      "why_script_flag_is_required": "VERIFIED, and it contradicts what the verb's name suggests: `nen bootstrap` does not fetch the script. It runs the checksum bootstrap rather than reimplementing it, so it needs bootstrap/nen.sh ON DISK — from a checkout carrying it (--repo) or named with --script / $NEN_BOOTSTRAP_SH. A Hatsu user has no nen checkout, so --script with the freshly fetched file is the form that works; without it the verb exits 7. Deliberate on nen's part: a compiled binary has no sibling bootstrap/ directory, and guessing one is how a nonexistent path becomes a silent failure.",
      "exit_codes": "The verb propagates the script's codes unchanged — a forced failure through the verb returns 6, verified — plus its own 7 above."
    }
  },
  "halt": {
    "when": "ONLY when the bootstrap itself fails (any non-zero exit above). An absent or out-of-range nen is not a halt — it is an auto-install.",
    "message_template": "Nen is unavailable and the checksum-verified bootstrap failed (exit ${EXIT_CODE} — ${EXIT_MEANING}). This operation is Nen-owned and will not be improvised. Run this yourself, then re-invoke:\n\n    curl -fsSL https://raw.githubusercontent.com/zheref/nen/v0.14.0/bootstrap/nen.sh -o /tmp/nen-bootstrap.sh\n    bash /tmp/nen-bootstrap.sh --ref v0.14.0\n\nTwo steps, never a pipe: the script reads ${BASH_SOURCE[0]} under `set -u`, so `curl … | bash` dies before it starts.\n",
    "after_halt": "Stop. Do not perform the Nen-owned operation by another route — not with raw gh, not with a shell equivalent, not from memory. The operation does not happen."
  },
  "no_improvised_fallback": {
    "rule": "There is no LLM-improvised fallback for a Nen-owned operation, ever.",
    "scope": "A Nen-owned operation is any operation for which a nen verb exists — readiness determination, backlog fetch and ordering, board assembly and render, gate derivation, label application, an issue or pull-request comment (`nen issue comment`), changelog collation, fan-out computation, method-block validation, perf comparison, a target repository's declared build/test/lint/coverage/deploy (`nen shu`), and the rest of `nen --help`.",
    "consequence": "If nen is unavailable and the bootstrap failed, the operation does not happen. Reporting that is the correct outcome; substituting a hand-rolled equivalent is not."
  },
  "no_jq": {
    "rule": "Nothing on this path shells out to jq, yq or python to read this file.",
    "why": "The ratified plan retires jq/yq as a DX friction — a machine needs one binary plus git and gh. And the reader here is an LLM agent that has already opened this file: parsing JSON by subprocess to hand values back to the entity that just read them buys a dependency for nothing.",
    "how_instead": "Read this file, take the literal values, and issue plain curl / bash / nen commands with those literals substituted in. The one machine that also reads it is nen itself — `nen schema check --repo <this checkout>` — and that is a validation of the file, never a way of reading values out of it."
  }
}
