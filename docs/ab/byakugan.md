# A/B evidence — `byakugan` (new skill, v0.24.0)

`claude/skills/byakugan/SKILL.md`: coverage capture and measurement at mukai, independently of
unit, UI and integration tests. Grammar `against [<base>]`. Never runs `nen shu test` or
`nen shu ui-test`.

**Phase ruling, 2026-09-14.** Coverage measurement and capture are this skill's. Historical
transcripts of gyo-as-coverage live in `docs/ab/gyo.md` and stay dated. Transcripts of
kotoamatsukami-as-coverage live in `docs/ab/kotoamatsukami.md` and stay dated.

The live extract verb, the `--touched --base` filter, per-file bands, and the seat
`not measurable here` were verified against those earlier records. This file does not re-run
them; it names the owner.
